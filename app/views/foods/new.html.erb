<div class="container width-50">
  <%= form_for @food, url: foods_path, local: true, data: { turbo: false }, method: :post do |f| %>
    <% if @food.errors.any? %>
      <div class="alert alert-danger">
        <ul>
          <% @food.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <h3 class="bg-body-tertiary rounded py-3">New Food Item</h3>
    <div class="mb-3">
      <%= f.label :name, class: "form-label" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>

    <div class="mb-3">
      <%= f.label :measurement_unit %>
      <%= f.text_field :measurement_unit, class: "form-control" %>
    </div>

    <div class="mb-3">
      <%= f.label :price %>
      <%= f.text_field :price, class: "form-control" %>
    </div>

    <div class="mb-3">
      <%= f.label :quantity %>
      <%= f.text_field :quantity, class: "form-control" %>
    </div>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <div>
      <%= f.submit "Save Food", class: "btn btn-secondary" %>
      <%= link_to "Cancel", foods_path, class: "btn btn-secondary cancel-link" %>
    </div>
  <% end %>
</div>